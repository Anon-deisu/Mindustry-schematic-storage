# Mindustry Schematic Storage

本仓库仅用于存储 Mindustry 蓝图（`.msch` 文件）。

- 蓝图文件在 `schematics/` 目录
- 这里只存文件，不包含插件/脚本等源码
- 仓库内容由机器人自动同步提交，请勿在此仓库内手动改动文件结构

---

This repository is only used to store Mindustry schematics (`.msch` files).
Files are under `schematics/`. No source code is hosted here.
